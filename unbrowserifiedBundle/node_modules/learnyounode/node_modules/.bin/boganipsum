#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../boganipsum/boganipsum" "$@"
  ret=$?
else 
  node  "$basedir/../boganipsum/boganipsum" "$@"
  ret=$?
fi
exit $ret
